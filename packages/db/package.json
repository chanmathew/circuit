{
  "name": "@circuit/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "pnpm rebuild better-sqlite3 && drizzle-kit migrate && pnpm --filter desktop rebuild:native",
    "db:reset": "node scripts/reset-dev-db.mjs && pnpm rebuild better-sqlite3 && drizzle-kit migrate && pnpm --filter desktop rebuild:native",
    "db:studio": "pnpm rebuild better-sqlite3 && drizzle-kit studio && pnpm --filter desktop rebuild:native"
  },
  "dependencies": {
    "better-sqlite3": "catalog:",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "catalog:",
    "@types/node": "catalog:",
    "drizzle-kit": "catalog:",
    "typescript": "catalog:"
  }
}
